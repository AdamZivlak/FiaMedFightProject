<Page
    x:Class="FiaMedFight.MenuScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FiaMedFight"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Include custom template, storyboard and style files here -->
                <ResourceDictionary Source="Templates/ControlStyleTemplates.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Rectangle Fill="Red" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2"/>
            <Rectangle Fill="Blue" Grid.Column="2" Grid.Row="0"/>

            <Rectangle Fill="Green" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1"/>
            <Rectangle Fill="Yellow" Grid.Column="2" Grid.Row="2" Grid.RowSpan="2"/>
        </Grid>

        <Button x:Name="ApplicationExitButton" Content="Exit Program"
                Style="{StaticResource MenuButtonStyle}" VerticalAlignment="Bottom" Click="ApplicationExitButton_Click" Width="241"/>

        <Button x:Name="NewGameButton" Content="New Game"
        Style="{StaticResource MenuButtonStyle}" VerticalAlignment="Bottom" Click="NewGameButton_Click" Width="241" Margin="610,0,0,707"/>

        <!-- this shows a hidden popup on current page -->
        <Button x:Name="RulesOpenButton" Content="Open Rules"
                Style="{StaticResource MenuButtonStyle}" VerticalAlignment="Bottom" HorizontalAlignment="Center" Click="RulesOpenButton_Click">
        </Button>
        
        <!-- this should be hidden until number of players are chosen -->
        <Button x:Name="GameStartButton" Visibility="Collapsed"  Content="Start Game" 
                Style="{StaticResource MenuButtonStyle}" Width="318" Height="130" Click="GameStartButton_Click" HorizontalAlignment="Center" Margin="0,682,0,0" VerticalAlignment="Top"/>

        <Grid x:Name="RulesPopup" Visibility="Collapsed" Height="500" Width="500" Background="BlueViolet">

            <TextBlock x:Name="RulesHeaderTextBlock" Text="Hej" HorizontalAlignment="Center" VerticalAlignment="Top" Width="500" />
            <TextBlock x:Name="RulesTextBlock" Text="Hopp" HorizontalAlignment="Center" VerticalAlignment="Center" Width="500" />

        </Grid>

    </Grid>
</Page>
